<li>
  <a href="#{{{anchorName}}}">{{>name no-return-sig=true}}</a>
  {{#each (children sortBy="scope,kind")}}
    {{#if @first}}<ul>{{/if}}
    {{>index~}}
    {{#if @last}}</ul>{{/if}}
  {{/each}}
</li>
