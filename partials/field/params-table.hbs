{{#if params~}}
| Param | Type | Description |
| ----- | ---- | ----------- |
{{#each params~}}
| {{{name}}} | {{{join (linkTo type.names style="code") " \| "}}} | {{{stripNewlines description}}} |
{{/each}}
{{/if}}